# Auditor (FastAPI brain) dependencies. Pinned loosely for hackathon speed; the collector
# and victim services have their own requirements.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
httpx>=0.27,<1.0            # SigNoz REST API client
clickhouse-driver>=0.2.7,<0.3   # native-protocol ClickHouse access (port 9000)
python-dotenv>=1.0,<2.0    # host-side .env loading for CLI runs
PyYAML>=6.0,<7.0           # fixgen reads/writes collector patch YAML
docker>=7.0,<8.0           # restart the collector container on apply/unapply
